<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>se.lnu.thesis</groupId>
    <artifactId>GoClusterViz</artifactId>
    <name>GoClusterViz</name>
    <packaging>jar</packaging>
    <version>1.0-RC</version>

    <developers>
        <developer>
            <name>Vladyslav Aleksakhin</name>
            <email>vladyslav.aleksakhinatgmail.com</email>
            <organization>Linneuniversitetet</organization>
            <organizationUrl>http://lnu.se</organizationUrl>
            <roles>
                <role>developer</role>
            </roles>
        </developer>
    </developers>

    <properties>
        <maven.build.timestamp.format>ddMMyyyy-HHmm</maven.build.timestamp.format>
    </properties>

    <dependencies>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.12</version>
        </dependency>

        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.5</version>
        </dependency>

        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>3.2</version>
        </dependency>

        <dependency>
            <groupId>net.sourceforge.collections</groupId>
            <artifactId>collections-generic</artifactId>
            <version>4.01</version>
        </dependency>

        <dependency>
            <groupId>com.google.collections</groupId>
            <artifactId>google-collections</artifactId>
            <version>1.0</version>
        </dependency>

        <dependency>
            <groupId>jung-algorithms</groupId>
            <artifactId>jung-algorithms</artifactId>
            <version>2.0.1</version>
        </dependency>

        <dependency>
            <groupId>jung-api</groupId>
            <artifactId>jung-api</artifactId>
            <version>2.0.1</version>
        </dependency>

        <dependency>
            <groupId>jung-graph-impl</groupId>
            <artifactId>jung-graph-impl</artifactId>
            <version>2.0.1</version>
        </dependency>

        <dependency>
            <groupId>jogl</groupId>
            <artifactId>jogl</artifactId>
            <version>1.1.1</version>
            <scope>provided</scope>
        </dependency>

    </dependencies>


    <build>

        <!--        <finalName>${project.artifactId}-${project.version}-${maven.build.timestamp}</finalName>-->
        <finalName>${project.artifactId}</finalName>

        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-Dfile.encoding=UTF-8</argLine>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>1.5</source>
                    <target>1.5</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <excludeScope>provided</excludeScope>
                            <excludeGroupIds>junit</excludeGroupIds>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.6</version>

                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <target>

                                <!-- CREATE ALL DIRECTORIES -->
                                <mkdir dir="${project.basedir}/build/current_release"/>
                                <mkdir dir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-win-i586"/>
                                <mkdir dir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-linux-i586"/>
                                <mkdir dir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-macosx-universal"/>
                                <!-- ****************************************************************************** -->

                                <!-- COPY NATIVE LIBRARY -->
                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-win-i586/native">
                                    <fileset dir="${project.basedir}/native/win-i586-native">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>

                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-linux-i586/native">
                                    <fileset dir="${project.basedir}/native/linux-i586-native">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>

                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-macosx-universal/native">
                                    <fileset dir="${project.basedir}/native/macosx-universal-native">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>
                                <!-- ****************************************************************************** -->

                                <!-- COPY JOGL AND GLUEGEN JARS -->
                                <copy
                                        file="${project.basedir}/lib/jogl.jar"
                                        toFile="${project.build.directory}/lib/jogl.jar">
                                </copy>

                                <copy
                                        file="${project.basedir}/lib/gluegen-rt.jar"
                                        toFile="${project.build.directory}/lib/gluegen-rt.jar">
                                </copy>
                                <!-- ****************************************************************************** -->

                                <!-- COPY JAVA LIBRARY -->
                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-win-i586/lib">
                                    <fileset dir="${project.build.directory}/lib">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>

                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-linux-i586/lib">
                                    <fileset dir="${project.build.directory}/lib">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>

                                <copy todir="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-macosx-universal/lib">
                                    <fileset dir="${project.build.directory}/lib">
                                        <include name="**/*"/>
                                    </fileset>
                                </copy>
                                <!-- ****************************************************************************** -->

                                <!-- COPY BUILD JAR -->
                                <copy
                                        file="${project.build.directory}/${project.artifactId}.jar"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-win-i586/${project.artifactId}.jar">
                                </copy>

                                <copy
                                        file="${project.build.directory}/${project.artifactId}.jar"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-linux-i586/${project.artifactId}.jar">
                                </copy>


                                <copy
                                        file="${project.build.directory}/${project.artifactId}.jar"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-macosx-universal/${project.artifactId}.jar">
                                </copy>
                                <!-- ****************************************************************************** -->

                                <!-- COPY LAUCHERS -->
                                <copy
                                        file="${project.basedir}/src/main/lauchers/run.bat"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-win-i586/run.bat">
                                </copy>

                                <copy
                                        file="${project.basedir}/src/main/lauchers/run.sh"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-linux-i586/run.sh">
                                </copy>


                                <copy
                                        file="${project.basedir}/src/main/lauchers/run.sh"
                                        toFile="${project.basedir}/build/current_release/${project.artifactId}-${project.version}-macosx-universal/run.sh">
                                </copy>
                                <!-- ****************************************************************************** -->

                                <!-- RENAME CURRENT BUILD DIRECTORY -->
                                <move
                                        todir="${project.basedir}/build/${project.artifactId}-${project.version}-${maven.build.timestamp}">
                                        <fileset dir="${project.basedir}/build/current_release"/>
                                </move>
                                <!-- ****************************************************************************** -->

                                <!-- DELETE CURRENT RELEASE BUILD DIRECTORY -->
                                <!--<delete dir="${project.basedir}/build/current_release"/>-->

                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            

        </plugins>
    </build>


</project>
